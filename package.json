{
  "name": "protato-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --serial install protato",
    "install": "npm-run-all --serial cd sandbox/library-genesis && npm install && cd ../node-starter && npm install && cd ../npm-sample-starter-module && npm install",
    "protato": "npx protato watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@alechp:servexyz/protato-demo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
